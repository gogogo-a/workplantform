# ==================== AI 模型配置 ====================

# Ollama 本地模型（GPU/CPU 使用由 Ollama 服务端自动决定）
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_DEFAULT_MODEL=llama3.2

# DeepSeek 云端模型
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 运行模式：控制 Embedding 和 Reranker 模型的设备
# 可选值: cpu, cuda, mps (macOS GPU)
# 注意：Ollama 模型的设备由 Ollama 服务端控制，此配置不影响 Ollama
RUNNING_MODE=cpu

# HuggingFace 模型配置
# 如果模型已下载到本地，设置为 1 可避免联网检查
# 如果需要从 HuggingFace 下载，可以使用镜像：HF_ENDPOINT=https://hf-mirror.com
TRANSFORMERS_OFFLINE=1
HF_HUB_OFFLINE=1

# ==================== 数据库配置 ====================

# MongoDB 配置
MONGODB_URL=mongodb://root:rootpassword@localhost:27017/
MONGODB_DATABASE=rag_platform

# Milvus 向量数据库配置
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_USER=root
MILVUS_PASSWORD=rootpassword
MILVUS_DB_NAME=rag_platform
MILVUS_COLLECTION_NAME=rag_documents

# Redis 缓存配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ==================== 邮件服务配置 ====================

# SMTP 邮件服务器配置（用于发送验证码）
EMAIL_HOST=smtp.qq.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@qq.com
EMAIL_HOST_PASSWORD=your_smtp_authorization_code
EMAIL_FROM=your_email@qq.com
EMAIL_TIMEOUT=10

# SSL 证书验证（开发环境可设为 False，生产环境建议设为 True）
# 如果遇到 "certificate verify failed" 错误，可以将此项设为 False
EMAIL_VERIFY_SSL=False

# ==================== 安全配置 ====================

# JWT 密钥（请使用强密码，建议 32 字符以上）
SECRET_KEY=your_secret_key_change_this_in_production_min_32_chars

# ==================== 其他配置 ====================

# Token 限制配置（用于聊天历史总结）
MAX_TOKEN_LIMIT_FOR_SUMMARY=6400

# 触发总结的消息数量阈值（超过此数量自动生成总结并保存为 send_type=2）
# 默认值：10（即每10条新消息生成一次总结）
SUMMARY_MESSAGE_THRESHOLD=10

